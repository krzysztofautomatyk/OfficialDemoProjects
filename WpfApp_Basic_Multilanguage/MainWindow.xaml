<Window
    x:Class="WpfApp_Basic_Multilanguage.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:WpfApp_Basic_Multilanguage"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="MainWindow"
    Width="800"
    Height="450"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ComboBox
            x:Name="LanguageComboBox"
            Grid.Row="0"
            Width="100"
            Margin="10"
            ItemsSource="{Binding AvailableLanguages}"
            SelectedItem="{Binding CurrentLanguage, Mode=TwoWay}"
            SelectionChanged="LanguageComboBox_SelectionChanged" />

        <TextBlock
            Grid.Row="1"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            FontSize="24"
            Text="{Binding Greeting}" />

        <StackPanel
            Grid.Row="2"
            Margin="10"
            HorizontalAlignment="Center"
            Orientation="Horizontal">
            <Button
                Width="100"
                Margin="5"
                Content="{Binding PolishButtonName}" />
            <Button
                Width="100"
                Margin="5"
                Content="{Binding EnglishButtonName}" />
        </StackPanel>
    </Grid>
</Window>